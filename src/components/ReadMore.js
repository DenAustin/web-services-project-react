import React from 'react'


const ReadMore = (props) => {
   return (
      <button className="button">
        <h4>{props.label} {props.RecentWorLabel}</h4>
      </button>
   )
}

export default ReadMore
